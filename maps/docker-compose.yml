services:
  mapserver:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: monitoreo_mapserver_standalone
    restart: unless-stopped
    volumes:
      - .:/data
    ports:
      - "8080:8080"
    command: [ "-p", "8080", "-c", "tileserver-config.json" ]
